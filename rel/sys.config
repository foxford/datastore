[
	{datastore, [
		%% iss => conf | {iss, kid} => conf
		{authentication, #{
			<<"idp.example.org">> =>
					#{keyfile => <<"keys/example.pem">>,
						verify_options => #{verify => [exp]}}
		}}
	]},
	{lager, [
		{error_logger_redirect, false}
	]}
].
